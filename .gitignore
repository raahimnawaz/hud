# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Environments — the lockfile is what pins versions, not a committed venv.
.venv/
venv/
env/

# Build artefacts
dist/
build/

# The demo GIF is generated by `vhs demo.tape`, not committed. It is large,
# binary, and rebuilding it is one command.
docs/demo.gif
docs/demo.mp4

# macOS
.DS_Store
